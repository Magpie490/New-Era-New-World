decisions = {
	organize_cricket_match = { #ruler selects a side to support, outcome is random, changed to be carib add future ones later like argentinians
		only_playable = yes
		ai_check_interval = 120
		
		potential = {
		    OR = {
				culture_group = franco_caribbean
				culture_group = caribeno
				culture_group = anglo_caribbean
				culture_group = niger_congolese 
				culture = minoan
				culture = bahamian
				culture = kairebiyan
				culture = maiamero
				culture = fluridani
				culture = neo_sefardi
			}
			is_playable = yes
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
		    NOT = { has_character_modifier = recent_cricket_game }
			NOT = { has_character_flag = cricket_in_progress }
			war = no
			in_command = no
			scaled_wealth = 0.5
			prestige = 25
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			NOT = {	trait = incapable }
			is_adult = yes
		}
		effect = {
			scaled_wealth = -0.5
			set_character_flag = do_not_disturb
			set_character_flag = recent_minor_decision
			hidden_tooltip = { 
				character_event = { id = indian_flavor.200 } 
			}
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 8 }
						}
					}
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					scaled_wealth = 5
				}
			}
			modifier = {
				factor = 0.5
				trait = shy
			}
		}
	}

	indian_feudal_coronation = {
		is_high_prio = yes
		only_playable = yes
		ai_check_interval = 24
		potential = {
			government = indian_feudal
			NOT = { has_character_flag = coronated_indian_feudal }
			NOT = {	trait = crowned_indian_feudal }
		}

		allow = {
			conditional_tooltip = {
				trigger = { 
					real_tier = BARON 
				}
				wealth = 25
			} 
			conditional_tooltip = {
				trigger = { 
					real_tier = COUNT 
				}
				wealth = 50
			} 
			conditional_tooltip = {
				trigger = { 
					real_tier = DUKE 
				}
				wealth = 100
			} 
			conditional_tooltip = {
				trigger = { 
					real_tier = KING 
				}
				wealth = 200
			} 
			conditional_tooltip = {
				trigger = { 
					real_tier = EMPEROR 
				}
				wealth = 400
			} 
			age = 21
            war = no
			in_command = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			NOT = {	trait = incapable }
		}

		effect = {
			if = {
				limit = {
					real_tier = BARON 
				}
				wealth = -25
			}
			if = {
				limit = {
					real_tier = COUNT 
				}
				wealth = -50
			}
			if = {
				limit = {
					real_tier = DUKE 
				}
				wealth = -100
			}
			if = {
				limit = {
					real_tier = KING 
				}
				wealth = -200
			}
			if = {
				limit = {
					real_tier = EMPEROR 
				}
				wealth = -400
			}
			custom_tooltip = {
				text = TOOLTIP_indian_feudal_coronation
				ROOT = {
				    narrative_event = { id = indian_flavor.100 }
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				wealth = 0
			}
			modifier = {
				factor = 1
				wealth = 400
			}		
		}
	}
} # decisions bracket. Do not mess with this.